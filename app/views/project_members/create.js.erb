<% if @project_member.new_record? %>
  // Not saved yet
<% else %>
  // Saved
  $("#addMember").modal('hide');
  $('#membersPartial').html('<%= j render(partial: "project_members/members_list", locals: { project_object: @project }) %>');
<% end %>